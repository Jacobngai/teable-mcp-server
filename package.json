{
  "name": "teable-mcp-server",
  "version": "1.0.0",
  "description": "Multi-tenant MCP server for Teable databases",
  "license": "MIT",
  "type": "module",
  "main": "dist/index.js",
  "bin": "dist/main.js",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "npm run build && node ./dist/main.js",
    "start:http": "npm run build && MCP_TRANSPORT=http node ./dist/main.js",
    "build": "tsc --project tsconfig.json",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.24.3",
    "@supabase/supabase-js": "^2.47.0",
    "express": "^5.1.0",
    "resend": "^4.0.0",
    "stripe": "^17.0.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/express": "^5.0.3",
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
