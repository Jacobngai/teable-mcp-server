services:
  openapi-mcp-service:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "${PORT:-8080}:${PORT:-8080}"
    environment:
      - OPENAPI_TOKEN=${OPENAPI_TOKEN}
      - PORT=${PORT:-8080}
      - SERVICE_FQDN_TEABLE_MCP_8080=true
